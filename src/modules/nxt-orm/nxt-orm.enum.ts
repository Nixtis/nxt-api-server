export enum NxtOrmEnum {
    // Relations
    ONE_TO_MANY,
    MANY_TO_ONE,
    MANY_TO_MANY,

    // Indexes
    PRIMARY_KEY,
    UNIQUE,

    // Types
    BOOLEAN,
    CHAR,
    DATETIME,
    ENUM,
    FLOAT,
    INT,
    JSON,
    TEXT,
    VARCHAR,
}
